{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/augentic/specify/schemas/config.schema.json",
  "title": "Specify config.yaml",
  "description": "Validates the structure of a Specify schema config file.",
  "type": "object",
  "required": ["schema", "context"],
  "additionalProperties": false,
  "properties": {
    "schema": {
      "type": "string",
      "description": "Schema URL or bare name (e.g., https://github.com/augentic/specify/schemas/omnia)."
    },
    "context": {
      "type": "string",
      "description": "Multi-line project context block (tech stack, architecture, etc.)."
    },
    "rules": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Per-blueprint rule overrides keyed by blueprint id. Each value is a multi-line string."
    }
  }
}
